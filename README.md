This is a simple app to determine whether or not a paint shop can successfully satisfy users paint requirements.
To run it on some sample input:
sbt run /multi-test.txt

The main method is in PaintShopApp 